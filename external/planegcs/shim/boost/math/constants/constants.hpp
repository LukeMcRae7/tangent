// Tangent shim: included by planegcs's Geo.h, which uses std::numbers instead
// of anything here. It does rely on the maths functions the real header brought
// in with it, so those come from here.
#pragma once
#include <cmath>
